CREATE TABLE PLAYERS (PLAYERID INT, FIRSTNAME VARCHAR, LASTNAME VARCHAR, WEIGHT INT, STARTYEAR INT);
SELECT P.PLAYERID FROM (SELECT PLAYERID, LASTNAME, FIRSTNAME, WEIGHT AS W FROM PLAYERS WHERE STARTYEAR <= 1982) P WHERE P.W >= 219;